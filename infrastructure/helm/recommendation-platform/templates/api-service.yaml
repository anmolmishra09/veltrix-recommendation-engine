apiVersion: v1
kind: Service
metadata:
  name: {{ include "recommendation-platform.fullname" . }}-api
  labels:
    {{- include "recommendation-platform.selectorLabels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "recommendation-platform.selectorLabels" . | nindent 4 }}